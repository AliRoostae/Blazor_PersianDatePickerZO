@inherits BaseDatePickerZO
<div class="picker-pack">
    <div class="picker-box">

      
        
            @if (PupupDatePickerZO)
            {
                <div class="input-group-picker">
                    <input type="text" value="@_dateFa" readonly/>
                    <span @onclick="ShowPicker">&#128197;</span>
                </div>
                if (_show)
                {<div class="frame @ThemePickerZO">
                    <div class="title">@_dateFa</div>
            <YearMonth @bind-SelectDate="@SelectDate" MaxDate="@MaxDate" MinDate="@MinDate" ThemePickerZO="@ThemePickerZO" />
            <DayZO @bind-SelectDate="@SelectDate" MaxDate="@MaxDate" MinDate="@MinDate" ThemePickerZO="@ThemePickerZO" />
            <TimeZO @bind-SelectDate="@SelectDate" MaxDate="@MaxDate" MinDate="@MinDate" ThemePickerZO="@ThemePickerZO" />
            <div class="btn-list">
                <button @onclick="ColsePopup">تأیید</button>
                <button @onclick="GetToday">امروز</button>
            </div> 
                </div>

            }
        }
        else
        {
            <div class="frame @ThemePickerZO">
                    <div class="title">@_dateFa</div>
            <YearMonth @bind-SelectDate="@SelectDate" MaxDate="@MaxDate" MinDate="@MinDate" ThemePickerZO="@ThemePickerZO" />
            <DayZO @bind-SelectDate="@SelectDate" MaxDate="@MaxDate" MinDate="@MinDate" ThemePickerZO="@ThemePickerZO" />
            <TimeZO @bind-SelectDate="@SelectDate" MaxDate="@MaxDate" MinDate="@MinDate" ThemePickerZO="@ThemePickerZO" />
            <div class="btn-list">
                <button @onclick="ColsePopup">تأیید</button>
                <button @onclick="GetToday">امروز</button>
            </div> 
                </div>
        }
           
        





    </div>
</div>

